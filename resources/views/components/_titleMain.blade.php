<div class="row mb-5">
    <div class="col-12 text-center">
        <h1 class="display-5 fw-bold text-soft">{{ $title }}</h1>
        @if(isset($summary))
            <p class="lead text-muted">{{ $summary }}</p>
        @endif
    </div>
</div>